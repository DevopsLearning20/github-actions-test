name : shell commands 

on: [push]

jobs:
  run-shell-commands:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "hello String"
      - name: multiple script
        run: |
          node -v
          npm -v
      - name: python commnad
        run: |
          import platform
          print(platfrom.processor())
        shell: python
